# server-web-extract

MCP server that extracts structured content from public web pages.

## Tools

- `extract_page`
- `extract_links`
- `extract_tables`

## Run

```bash
node server.mjs
```

This server is designed for one-shot stdio invocation (bridge spawns one process per JSON-RPC request).
