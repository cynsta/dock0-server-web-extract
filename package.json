{
  "name": "@dock0-labs/server-web-extract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for extracting clean text, links, and tables from web pages.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
